1. user can't login in 2 device (the other one will automatically logout).
    - create user_session value, and insert in db after signup and session every login.
2. define user roles and access control (normal vs admin)
    - who can have full access(edit, delete, update, insert).
3. limit the feedback form input to 5 per day to minimize spam.
4. make sure email is used once per user
5. password must atleast 8 chars
6. use sql prepared statement to avoid sql injection
7. filter the input fields
8. add user login attempts or user_activity to view 
9. added captcha to invalidate login that looks suspicious to avoid spam
10. disable the login btn after 3 error attempts for 30 seconds
11. prevent Session hijacking by checking the token in session and the session in database every login
12. Force logout if session token !true db token value


security:
1. login - CAPTCHA, login attempt limits, device lock, password hashing,email filtering, session token, and logging.


$captchaResponse = $_POST['cf-turnstile-response'] ?? '';

// ---------- CAPTCHA CHECK ----------
if (empty($captchaResponse)) {
    $errors[] = 'Please complete the CAPTCHA.';
} else {
    $secretKey = '0x4AAAAAACWH4cOK8wYy8t7Fxb6wV3helSU';
    $verify = file_get_contents("https://challenges.cloudflare.com/turnstile/v0/siteverify", false, stream_context_create([
        'http' => [
            'method' => 'POST',
            'header' => "Content-type: application/x-www-form-urlencoded",
            'content' => http_build_query([
                'secret' => $secretKey,
                'response' => $captchaResponse,
                'remoteip' => $ipAddress
            ]),
        ],
    ]));
    $responseData = json_decode($verify);
    if (!$responseData->success) {
        $errors[] = 'CAPTCHA verification failed. Please try again.';
    }
}

<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>

<div class="cf-turnstile mt-3" data-sitekey="0x4AAAAAACWH4cPoBQCrb7lD"></div>
